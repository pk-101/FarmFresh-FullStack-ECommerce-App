const Footer = () => {
  return (
    <footer className="bg-gray-100 text-center text-sm text-gray-500 py-3">
      © {new Date().getFullYear()} FarmFresh — Fresh from farmers
    </footer>
  );
};

export default Footer;